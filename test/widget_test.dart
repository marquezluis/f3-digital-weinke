// test/widget_test.dart
// Placeholder smoke test for Digital Weinke.
//
// Flutter creates a default counter-app widget_test.dart that references
// `MyApp`. This project uses `DigitalWeinke` instead, so this file prevents
// the generated default test from breaking `flutter test`.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('Digital Weinke test harness is configured', () {
    expect(true, isTrue);
  });
}
